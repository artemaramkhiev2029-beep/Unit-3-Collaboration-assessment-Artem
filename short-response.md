# Short Response Questions

1. Create new branch by `git branch (name)` and swith to that branch by `git checkout (name)`
2. Because if they work on the same branch like main, **they will get a error**
3. *Git* is a **command line,tool,control system**. *Github* is a **website and cloud-based service that helps developers store and manage their code**. Git helps to github work because in a github(website) developers need to use a command line(Git).
4. git add .   
git commit -m "saving work"  
git push